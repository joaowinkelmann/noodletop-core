{
  "name": "noodletop-ws",
  "version": "0.0.1",
  "author": "joaowinkelmann",
  "description": "WebSocket tabletop manager",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "bun dev",
    "build": "bun build ./src/index.ts --target=bun --outfile=websocket.ts",
    "start": "bun run ./websocket.ts"
  },
  "dependencies": {
    "bun": "^1.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.0.7",
    "@types/node": "^18.15.11"
  }
}
